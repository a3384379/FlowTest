# PR #100 审查修复

开发验收记录中的提交前状态及 377 项测试结果为首次交付时点快照。提交和合并状态以 GitHub PR #100 为准。

本轮处理三个审查问题：

1. 节点表单处于干净状态时，撤销或外部节点变化会同步基线和表单，并重建原始字段编辑实例；脏会话仍保留输入并执行冲突保护。新增“应用 → 撤销 → 表单恢复 → 再次编辑”组件及真实浏览器回归。
2. 请求预览失败保留标准 API 错误原因和追踪 ID；表单校验失败使用中文提示。新增拒绝预览错误信封回归。
3. 无效 JSON 使用中文语法提示，保留原始输入；不直接显示英文 V8 解析信息。

本地定向测试 13 项通过，Compose Playwright 10 项通过。全量运行的一项页面用例曾超时，独立复跑两次通过；不并发运行构建/浏览器后再次执行全量回归，最终结果以本 PR 最新提交的 CI 为准。

原用户修改 deploy/ruoyi/compose.yaml 不包含在 PR 中。
