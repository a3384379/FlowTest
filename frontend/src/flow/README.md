# Flow editor

React Flow 节点、边、画布交互及运行状态可视化放在这里。工作流持久化契约由前后端共同评审后冻结。

S16 画布约束：

- SubFlow/ForEach 只选择已发布版本，当前工作流不会出现在自己的候选列表。
- 复制粘贴必须生成新节点 ID；撤销/重做只记录定义变化，选择态不进入历史。
- 自动布局只更新坐标，不改变节点配置、边或稳定节点 ID。
- ForEach 元素由安全 JMESPath 从上游输出取得，并通过子流程 Start 输出的字段映射继续传递。
